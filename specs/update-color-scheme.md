# Apply branded colors to the UI

- Try to use `#04bd88` as a primary color in both dark and light mode.

```
// ignore for now,
// - Try `rgb(119 255 216)` as a primary color in dark mode.
```

## Implementation Details

Convert the colors using makeshift node scripts.
